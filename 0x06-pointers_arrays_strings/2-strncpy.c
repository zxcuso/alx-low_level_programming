#include "main.h"

/**
 * _strncpy - c function
 * terminating null byte
 * if the lenght of the source 
 * the reminder of the 
 * works identically to the standard
 * @dest: buffer
 * @src: the  source
 * @n: max number
 * Return: rturn
 */

int _strcmp(char *s1, char *s2)
{
	int i;
	
	for (i = 0; i < n && src[i] != '\0'; i++)
		dest[i] = src[i];

	for ( ; i < n; i++)
		dest[i] = '\0';

	return (dest);
}
